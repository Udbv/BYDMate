## BYDMate Waze 3.15.0-dev.5 (development pre-release)

**Development channel only.** Follow-up to dev.4 from the same car session.

### Fixed
- The arrow blinked off the glass after 30 s whenever the car stood still: Waze redraws nothing,
  no accessibility event arrives, no read happens, and the maneuver expires. A keep-alive re-read
  of the Waze window now runs 20 s after the last read while guidance is active.
- Toggling an AR-HUD channel re-opens the HUD session; the old session's instrument reset is now
  awaited so its "stopped" cannot land after the new session's "active".

### Still to check
- Whether "Factory-map context" and "HUD frame" can stay off (the glass reads the instrument
  features); the log from 08:54 shows the arrow data flowing with the context off.
