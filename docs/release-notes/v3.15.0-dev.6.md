## BYDMate Waze 3.15.0-dev.6 (development pre-release)

**Development channel only.**

### Changed
- Tang L result: the glass reads only "Guidance to the instrument panel". It stays on; "HUD frame"
  and "Factory-map context" are now off by default (switches kept for other DiLink 150 trims).

### Carried over from dev.5
- Keep-alive re-read of the Waze window while parked (arrow no longer blinks off after 30 s).
- Awaited instrument reset when a channel is toggled.

### Next
- Road name on the glass needs a byte-array feature write (1140461576) from the helper daemon.
